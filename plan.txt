
- [x] force function type
    - [x] allow forcing a function type (instead of fetching it automatically from TypeSet)
    - [x] do so when using the "Force type..." button
- [x] render $argN values as the parameter name if known, then inline them everywhere
    - creates the illusion that the argument IS a binding
- [x] gui: fix highlighting between phi and upsilon

- [ ] "view features" paradigm:
    - instead of separate panels for asm, cfg, ast,
    - we get a single view and three toggles for asm, cfg, ast;
        - when asm: just the assembly, linear
        - when cfg: just a graph for the cfg with the block numbers
        - when ast: just the ast without explicit cfg repr or asm

        - when asm|cfg: the cfg view is filled in with asm fragments
        - when cfg|ast: the cfg view is filled in with AST fragments

        - when asm|ast: the ast view has asm fragments on the side, but the code is still laid out in structured programming

        - when asm|cfg|ast: cfg view filled in with asm and AST fragments side by side

- [ ] advanced block order customization
    - [ ] blocks can be duplicated
    - [ ] blocks can be hidden
        - these must still be "archived" somewhere so that they can be pulled out again

    - [ ] context menu:
        - "duplicate to here"
        - "move to here" (blocks must not have duplicates)

- [ ] use type info: enum values
    - initialize with DWARF, allow user edits

- [ ] loops
    - think about them for a little while
    - [ ] ui?
    - [ ] heuristically mark them

- [ ] string literals
- [ ] select parent AST node (corresponding value etc.)

- [ ] gui: related log messages to reg
- [ ] gui: related log messages to block/label
- [ ] attach errors/warnings to ssa instructions wherever applicable
    - reasonable approximation: create a panel for tracing that allows searching,
    also based on language elements (registers, types, etc.)

- [ ] gui: export to non-interactive text

--- low prio

- [ ] gui: multiple functions (tabs?)

- [ ] remove horizontal padding/margin in ast's Flow seqs

- [ ] multiple views over the same function?
    - cross-view highlighting or highlight per view?
        - when hovering a register/block, highlight every corresponding item in every view (both same view and the others)

- [ ] better syntax for deref
